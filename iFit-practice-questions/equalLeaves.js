/*
 * Create a function that takes a tree structure and returns 1
 * if all leaf values are equal. Return 0 if not all leaves are equal.
 */

const sampleTree = {
  'a': 1,
  'b': 1,
  'c': {
    'c.1': 1,
    'c.2': {
      'c.2.i': 1,
      'c.2.ii': 1,
    },
  },
};

const leavesAreEqual = (tree) => {
  // ...
};
